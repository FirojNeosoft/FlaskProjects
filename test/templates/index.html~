<!DOCTYPE html>
<html lang = "en">
   <head></head>
   <body>
      
      <h3>
         <a>Comments - Flask Example</a>
      </h3>
      
      <hr/>
      {%- for message in get_flashed_messages() %}
         {{ message }}
      {%- endfor %}
		
      <h3>Students (<a href = "{{ url_for('contact') }}">Add Student
         </a>)</h3>
      
      <table>
         <thead>
            <tr>
               <th>Name</th>
               <th>Gender</th>
               <th>Email</th>
               <th>Address</th>
               <th>Age</th>
               <th>Language</th>
            </tr>
         </thead>
         
         <tbody>
            {% for student in students %}
               <tr>
                  <td>{{ student.name }}</td>
                  <td>{{ student.gender }}</td>
                  <td>{{ student.email }}</td>
                  <td>{{ student.address }}</td>
                  <td>{{ student.age }}</td>
                  <td>{{ student.lang }}</td>
               </tr>
            {% endfor %}
         </tbody>
      </table>
      
   </body>
</html>
